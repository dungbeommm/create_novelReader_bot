# Runtime dependencies (pinned for reproducible GitHub Actions / Docker builds).
# NOTE: piper-tts 1.2.0 ships wheels only for Python <= 3.10, so the whole
# project targets Python 3.10 in CI and Docker.
piper-tts==1.2.0
pydantic==2.7.4
pydantic-settings==2.3.4
PyYAML==6.0.1
python-dotenv==1.0.1
beautifulsoup4==4.12.3
lxml==5.2.2
EbookLib==0.18
mutagen==1.47.0
Pillow==10.4.0
python-telegram-bot==21.4
httpx==0.27.0
tenacity==8.5.0
py7zr==0.21.1
rarfile==4.2
