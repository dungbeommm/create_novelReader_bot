# Optional per-voice display metadata.
#
# Voices are auto-discovered from the models/ directory; this file only lets you
# override the friendly name or language shown in the Telegram menu. It is
# entirely optional -- delete it and everything still works.

voices:
  ngoc_huyen:
    display_name: "Ng\u1ecdc Huy\u1ec1n"
    language: vi
  # model_a:
  #   display_name: "Model A"
  #   language: vi
